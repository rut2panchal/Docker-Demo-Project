node {

  agent any
  stage('Build') {
            steps {
                echo 'building docker image'
            }
        }
  
  stage('Test') {
            steps {
                echo 'Testing docker image'
            }
        }
  stage('Deploy') {
            steps {
                echo 'Deloying docker image'
            }
        }
}
